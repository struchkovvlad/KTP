# Завдання 2
# Напишіть програму мовою Python,
# яка містить функцію equation(a,b),
# що розв'язує лінійне рівняння a⋅x+b=0
# та виводить на друк відповідь з точністю
# два знаки після коми, або текст nema,
# якщо рівняння не має розв'язку або слово bezlich,
# якщо будь-яке значення x є розв'язком даного рівняння.

# Приклад виклику функції та відповідь для такого виклику наведені нижче. 

# Приклад: Тестовий виклик: equation(2,-4)
# Результат: 2.0

def equation(a, b):
    strA = float(a)
    strB = float(b)
    if strA == 0:
        return 'nemae'

    if strB == 0:
        return 'bezlich'

    if strA!=0 and strB!=0:
        return float(a) / float(b)


InA = input("Введіть значення А: ")
InB = input("Введіть значення B: ")

InStr = 'з рівняння a*x - b = 0 маємо Х ='
print(InStr + str(equation(InA, InB)))
